
type Hotel {
    name: String!
    freeServices: [String]!
    rate: Int!
    price: Int
    reviewScore: Float!
    totalReviewCount: Int!
    imageUrl: String!
    id: Int!
}

type Price {
    id: String
    price: Int
}
type Query {
    hotels: [Hotel]!
    prices(id:String): Price!
}